---
import { Text } from '@/components/typography';
import type { ComponentProps } from 'astro/types';

type Props = Omit<ComponentProps<typeof Text<'h4'>>, 'as'>;
---

<Text data-slot="note-title" as="h4" size="inherit" trim {...Astro.props}><slot /></Text>
